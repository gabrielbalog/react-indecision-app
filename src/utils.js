console.log('utils.js is running');

export const square = (x) => x * x;
export const add = (a, b) => a + b;
export default (a, b) => a - b; // shorthand para exportar funcao como default


// export { square, subtract as default };
